# Set all packages from download.docker.com to the priority of backports/local packages.
# This enables automatic updates, but doesn't override system packages.
Package: *
Pin: origin download.docker.com
Pin-Priority: 100
